import paths from '@/constants/paths';
import type { RouteConfig } from '@/routes/_types';
const CustomersRoute: RouteConfig[] = [
  {
    name: 'CustomersRoute',
    path: paths.customers,
  },
];
export default CustomersRoute;
