import paths from '@/constants/paths';
import type { RouteConfig } from '@/routes/_types';
const ReportRoute: RouteConfig[] = [
  {
    name: 'ReportRoute',
    path: paths.report,
  },
];
export default ReportRoute;
